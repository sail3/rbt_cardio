{% extends 'base.html.twig' %}

{% block body %}
    <h1>CausaDescompensante</h1>

    <table>
        <tbody>
            <tr>
                <th>Idcausadescompensante</th>
                <td>{{ causaDescompensante.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ causaDescompensante.nombre }}</td>
            </tr>
            <tr>
                <th>Descripcion</th>
                <td>{{ causaDescompensante.descripcion }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('causa_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('causa_edit', { 'id': causaDescompensante.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
